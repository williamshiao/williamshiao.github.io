// Contact destinations — read by the Contact panel (see
// ../components/sections/Contact.tsx) and, previously, by the two separate
// shape shapes that opened them directly. Both are now folded into the one
// "Contact" pageId shape (see ../components/physics/shapes.ts), which opens
// a panel instead of jumping straight to a mailto:/new tab.

// TODO(content): replace with your real email address.
export const CONTACT_EMAIL = "hello@williamshiao.dev";
// TODO(content): replace with your real GitHub (or preferred code host) profile URL.
export const GITHUB_URL = "https://github.com/your-username";
